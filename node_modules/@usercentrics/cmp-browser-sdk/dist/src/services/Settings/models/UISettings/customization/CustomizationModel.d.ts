import { FirstLayerCustomization, FontCustomization, LegacySettings, LogoCustomization, ToggleCustomization } from '../../../interfaces';
import { THEME } from '../../../enums';
import ColorCustomization from './ColorModel';
declare class Customization {
    button: {
        borderRadius: string;
    };
    color: ColorCustomization;
    firstLayer: FirstLayerCustomization;
    font: FontCustomization;
    layer: {
        borderRadius: string;
    };
    logo: LogoCustomization;
    overlay: {
        opacity: number;
    };
    privacyButton: {
        backgroundColor: string | null;
        desktopSize: number | null;
        iconColor: string | null;
        mobileSize: number | null;
    };
    theme: THEME;
    toggle: {
        active: ToggleCustomization;
        disabled: ToggleCustomization;
        inactive: ToggleCustomization;
    };
    useBackgroundShadow: boolean;
    constructor(legacyCustomization: LegacySettings);
}
export default Customization;
