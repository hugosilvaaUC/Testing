import { CONSENT_ACTION, CONSENT_TYPE, TRACKING_EVENT_TYPE } from '../enums';
import { StorageCcpa, StorageTCF } from '../../Storage/interfaces';
import { UserConsentResponse } from '../../Api/interfaces';
export interface Consent {
    action: CONSENT_ACTION;
    status: boolean;
    type: CONSENT_TYPE;
}
export interface ConsentHistory extends Consent {
    language: string;
    timestamp: number;
    versions: {
        application: string;
        service: string;
        settings: string;
    };
}
export interface UserSessionData {
    consents: UserConsentResponse[];
    controllerId: string;
    language: string;
    tcf?: StorageTCF;
    ccpa?: StorageCcpa;
}
export interface TrackEventParameters {
    eventType: TRACKING_EVENT_TYPE;
}
