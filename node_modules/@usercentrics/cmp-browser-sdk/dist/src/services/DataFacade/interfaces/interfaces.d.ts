import { Consent } from './interfaces.public';
import { Consent as ServiceConsent } from '../../Settings/interfaces';
export interface DataTransferObject {
    applicationVersion: string;
    consent: Consent;
    service: {
        id: string;
        name: string;
        processorId: string;
        version: string;
    };
    settings: {
        controllerId: string;
        id: string;
        language: string;
        referrerControllerId: string;
        version: string;
    };
    timestamp: number;
}
export interface DataFacadeService {
    consent: ServiceConsent;
    id: string;
    name: string;
    processorId: string;
    version: string;
}
