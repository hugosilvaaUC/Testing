import { CategoryData, DefaultData } from './interfaces';
import { LegacyHashService, LegacySettings } from '../../Settings/interfaces';
declare class Default implements DefaultData {
    categories: CategoryData[];
    controllerId: string;
    constructor(legacySettings: LegacySettings, controllerId: string, legacyHashServices?: LegacyHashService[]);
}
export default Default;
