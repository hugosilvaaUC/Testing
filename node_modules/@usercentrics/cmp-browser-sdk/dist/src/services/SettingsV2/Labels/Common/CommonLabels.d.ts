import { CategoryLabels, CommonLabels, LinksLabels } from '../interfaces';
import { DefaultGeneralLabels, DefaultServiceLabels, LegacyHashService, LegacySettings, Translations } from '../../../Settings/interfaces';
import { CookieInformationLabels } from '../../../Settings/models';
import ServiceLabels from './ServiceLabels';
declare class Common implements CommonLabels {
    categories: CategoryLabels[];
    cookieInformation: CookieInformationLabels;
    general: DefaultGeneralLabels;
    links: LinksLabels;
    poweredBy: string;
    service: DefaultServiceLabels;
    services: ServiceLabels[];
    constructor(legacySettings: LegacySettings, translations?: Translations | null, legacyHashServices?: LegacyHashService[]);
}
export default Common;
