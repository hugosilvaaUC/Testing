import { ButtonColor } from '../interfaces';
import { LegacySettings } from '../../../Settings/interfaces';
declare class ButtonsCustomization {
    accept: ButtonColor;
    borderRadius: string;
    deny: ButtonColor;
    more: ButtonColor;
    privacy: {
        backgroundColor: string | null;
        desktopSize: number | null;
        iconColor: string | null;
        mobileSize: number | null;
    };
    save: ButtonColor;
    constructor(legacySettings: LegacySettings);
}
export default ButtonsCustomization;
