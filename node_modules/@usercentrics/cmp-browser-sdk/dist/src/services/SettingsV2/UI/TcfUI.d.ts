import { TCFButtonsConfig, TCFFirstLayerConfig, TCFSecondLayerConfig } from './interfaces';
import Common from './Common';
import { LegacySettings } from '../../Settings/interfaces';
import { TCF_SCOPE } from '../../..';
import { TcfUI } from '.';
declare class Tcf extends Common implements TcfUI {
    buttons: TCFButtonsConfig;
    firstLayer: TCFFirstLayerConfig;
    scope: TCF_SCOPE;
    secondLayer: TCFSecondLayerConfig;
    constructor(legacySettings: LegacySettings);
}
export default Tcf;
