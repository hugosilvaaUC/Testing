import { CornerPosition } from '../Core/interfaces';
import { LegacySettings } from '../../Settings/interfaces';
declare class PrivacyButtonUI {
    iconUrl: string | null;
    position: CornerPosition;
    constructor(legacySettings: LegacySettings);
}
export default PrivacyButtonUI;
