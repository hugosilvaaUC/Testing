import { CcpaButtonsConfig, CcpaFirstLayerConfig, CcpaSecondLayerConfig } from './interfaces';
import { CcpaUI } from '.';
import Common from './Common';
import { LegacySettings } from '../../Settings/interfaces';
declare class Ccpa extends Common implements CcpaUI {
    buttons: CcpaButtonsConfig;
    firstLayer: CcpaFirstLayerConfig;
    secondLayer: CcpaSecondLayerConfig;
    constructor(legacySettings: LegacySettings);
}
export default Ccpa;
